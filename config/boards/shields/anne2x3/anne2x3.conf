# anne2x3.conf

# Habilitar el backend SPI para WS2812
CONFIG_WS2812_STRIP_SPI=y

# Deshabilitar explícitamente otros posibles backends de la misma choice
# (si existen y sus nombres son estos)
# Esto es para asegurar que la 'choice' se resuelva limpiamente a SPI.
# CONFIG_WS2812_STRIP_GPIO=n
# CONFIG_WS2812_STRIP_PWM=n

# Asegurar que el subsistema de tiras de LED general esté habilitado
CONFIG_LED_STRIP=y